// About page — placeholder
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "About Us | Kashmir Cambridge",
    description: "Learn more about the history, mission, and vision of Kashmir Cambridge.",
    keywords: ["about us", "history", "mission", "vision"],
};

export default function AboutPage() {
    return null;
}
