// Events page — placeholder
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Events | Kashmir Cambridge",
    description: "Discover the latest events at Kashmir Cambridge",
    keywords: ["events", "calendar", "university events"],
};

export default function EventsPage() {
    return null;
}
